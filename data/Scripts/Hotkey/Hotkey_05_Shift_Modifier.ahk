#Requires AutoHotkey v2.0
#SingleInstance Force

/**
* Shift Modifier (+)
* + represents the Shift key
*/

; Shift+F5 to refresh with cache clear
+F5:: {
    MsgBox("Shift+F5 pressed`n`nThis could trigger a 'hard refresh' action")
}
